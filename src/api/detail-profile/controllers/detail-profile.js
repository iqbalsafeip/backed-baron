'use strict';

/**
 *  detail-profile controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::detail-profile.detail-profile');
